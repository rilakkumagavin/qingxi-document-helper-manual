const printButton=document.querySelector('#print');printButton.addEventListener('click',()=>{const entries=[...document.querySelectorAll('details')];const state=entries.map(e=>e.open);entries.forEach(e=>e.open=true);window.addEventListener('afterprint',()=>entries.forEach((e,i)=>e.open=state[i]),{once:true});window.print()});if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting){document.querySelectorAll('nav a').forEach(a=>{if(a.hash==='#'+entry.target.id)a.setAttribute('aria-current','location');else a.removeAttribute('aria-current')})}}},{rootMargin:'-15% 0px -65% 0px'});document.querySelectorAll('section[id]').forEach(s=>observer.observe(s))}